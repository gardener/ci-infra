apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: peribolos-config
  namespace: flux-system
spec:
  interval: 30m
  path: config
  prune: true
  retryInterval: 2m
  sourceRef:
    kind: GitRepository
    name: org
  timeout: 3m
  wait: true
