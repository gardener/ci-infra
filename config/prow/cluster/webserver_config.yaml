apiVersion: v1
kind: ConfigMap
metadata:
  name: webserver-config
  namespace: prow
data:
  data.conf: |-
    server {
       listen      80;
       server_name  _;
       location / {
           root /data;
       }
    }
